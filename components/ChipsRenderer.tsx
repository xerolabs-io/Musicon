import React from 'react'
import { getHomeData } from '@/actions/home.actions'

const ChipsRenderer = async () => {

    // const homeData = await getHomeData()
    // console.log(homeData);


    return (
        <div>Chips</div>
    )
}

export default ChipsRenderer